package me.camdenorrb.crescentvm.lexer

enum class CrescentTokenType {
    IF,
    ELSE,
    WHEN,
    WHILE,
    VALUE,
    TYPE,
    RETURN,
    FUN,
    VAR,
    VAL,
    STRUCTURE,
    END_BLOCK
}