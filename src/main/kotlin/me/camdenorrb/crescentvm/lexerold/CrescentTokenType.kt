package me.camdenorrb.crescentvm.lexerold

enum class CrescentTokenType {
    IF,
    ELSE,
    WHEN,
    WHILE,
    VALUE,
    TYPE,
    RETURN,
    FUN,
    VAR,
    VAL,
    STRUCTURE,
    END_SCOPE     // }
}